defmodule PeekHomeWeb.Resolvers do
  def list_orders(_parent, _args, _resolution) do
    {:ok, nil}
  end

  def list_payments(_parent, _args, _resolution) do
    {:ok, nil}
  end

  def create_order(_parent, _args, _resolution) do
    {:ok, nil}
  end

  def make_payment(_parent, _args, _resolution) do
    {:ok, nil}
  end

  def order_and_pay(_parent, _args, _resolution) do
    {:ok, nil}
  end
end
